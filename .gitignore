# checkpoints — large binaries
checkpoints*/
*.pt
*.pth

# python
__pycache__/
*.pyc
*.pyo
venv/
*.egg-info/
dist/
build/

# data outputs (kept locally, too large for git)
geovit_model/
figures/
report/*.csv    # per-image result tables (MB-scale); summary .txt files are tracked
